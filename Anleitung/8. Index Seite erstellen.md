Datei:

```
src/pages/index.astro
```

Code:

```
---
import { getCollection } from 'astro:content';

const notes = await getCollection('notes');
---

<h1>Obsidian Notizen</h1>

<ul>
  {notes.map(note => (
    <li>
      <a href={`/notes/${note.slug}`}>
        {note.slug}
      </a>
    </li>
  ))}
</ul>
```

[[9. Git Repository erstellen]]
[[(1.) Inhaltsverzeichnis]]