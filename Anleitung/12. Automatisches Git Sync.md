Script erstellen:

```
sync.sh
```

```
git add .
git commit -m "auto update"
git push
```

---

# Linux / Mac Cronjob

```
crontab -e
```

Eintrag:

```
*/10 * * * * cd /pfad/zum/repo && ./sync.sh
```

---

# Windows Task Scheduler

Batch Datei:

```
sync.bat
```

```
git add .
git commit -m "auto update"
git push
```

Task Scheduler:

- Trigger: Alle 10 Minuten
- Aktion: sync.bat ausführen

[[13. Erweiterungen]]
[[(1.) Inhaltsverzeichnis]]