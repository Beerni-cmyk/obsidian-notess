Git initialisieren:

```
git init
git add .
git commit -m "Initial commit"
```

GitHub Repository erstellen:

```
obsidian-astro-site
```

Verbinden:

```
git remote add origin https://github.com/USER/obsidian-astro-site.git
git branch -M main
git push -u origin main
```

[[10. GitHub Actions konfigurieren]]
[[(1.) Inhaltsverzeichnis]]